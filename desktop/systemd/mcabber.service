[Unit]
Description=Client mcabber for xmpp
After=i3-gaps.target
PartOf=i3-gaps.target

[Service]
Type=simple

Environment="DISPLAY=:0"
ExecStart=/usr/bin/termite --class mcabber-desktop -e mcabber

Restart=always
RestartSec=5

RemainAfterExit=no

[Install]
WantedBy=i3-gaps.target
