set preview_images true
default_linemode devicons
set draw_borders true

map gd cd ~/Downloads
map gD cd ~/rtorrent/downloads
map gs cd ~/screens
map go cd ~/other
map gC cd ~/.config
map gp cd ~/build/
map gt cd ~/tmp/

map xx shell -fs termite
map xt shell -fs thunar
map xn shell -fs termite -e "zsh -c 'nvim %s; zsh -i'"
map xd shell -fs dragon-drag-and-drop %p
